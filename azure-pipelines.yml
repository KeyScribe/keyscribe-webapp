# Starter pipeline
# Start with a minimal pipeline that you can customize to build and deploy your code.
# Add steps that build, run tests, deploy, and more:
# https://aka.ms/yaml

trigger:
- none

jobs:
- deployment: VMDeploy
  displayName: 'Deploy to Azure VM'
  environment:
    name: keyscribe-azure-vm
    resourceType: VirtualMachine
  strategy:
    runOnce:
      deploy:
        steps:
        - task: Docker@2
          displayName: 'Pull Backend'
          inputs:
            containerRegistry: 'DockerHubKeyscribe'
            command: 'build'
            Dockerfile: '**/Dockerfile'
        - task: AzureCLI@2
          displayName: 'Run Docker Compose on VM'
          inputs:
            azureSubscription: 


  
